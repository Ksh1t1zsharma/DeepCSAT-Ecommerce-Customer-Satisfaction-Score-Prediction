# Models

This directory is intended to store trained model artifacts (state dicts, scalers, exported ONNX models, etc.). For reproducibility, do not commit large binary weights to the main branch. Instead, store models in a release asset, cloud storage, or a model registry.
